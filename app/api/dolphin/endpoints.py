"""Dolphin API endpoints and paths.

Keep all route fragments in one place for discoverability and future reuse.
"""
from __future__ import annotations

BASE_ACCOUNTS = "/accounts"
ACCOUNT_BY_ID = "/accounts/{account_id}"
STATISTICS = "/statistics"
STATUS = "/status"
TEST = "/ping"
